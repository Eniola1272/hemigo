"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Field, Select, Textarea } from "@/components/ui/form-field";
export function CopywritingForm({windowId,firstFree}:{windowId?:string;firstFree:boolean}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/dashboard/copywriting",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({windowId,audience:form.get("audience"),tone:form.get("tone"),notes:form.get("notes")})});const body=await response.json();if(!response.ok){setError(body.error??"Could not submit request.");setBusy(false);return}setMessage("Your brief is in. A copywriter can now review this launch.");setBusy(false)}if(message)return <div className="card p-8 text-center"><h2 className="text-2xl font-bold">Request received.</h2><p className="mt-2 text-slate-500">{message}</p><Button href="/dashboard/windows" className="mt-5">Back to Hemigos</Button></div>;return <form onSubmit={submit} className="card p-6 sm:p-8"><div className="rounded-[12px] bg-indigo-50 p-5"><p className="font-bold text-indigo-900">{firstFree?"Your first request is free.":"Launch better with expert copy."}</p><p className="mt-1 text-sm text-indigo-700">The brief covers your storefront headline, product copy, WhatsApp launch message, Instagram caption and CTA.</p></div><div className="mt-6 grid gap-5"><Field label="Who is this launch for?"><Textarea name="audience" required placeholder="Busy professionals in Lagos who want a proper Sunday meal…"/></Field><Field label="Desired tone"><Select name="tone"><option>Warm and trustworthy</option><option>Bold and energetic</option><option>Premium and refined</option><option>Playful and conversational</option></Select></Field><Field label="Anything else the copywriter should know?"><Textarea name="notes" placeholder="What makes this offer special, words to avoid, launch channels…"/></Field></div>{error&&<p className="mt-4 text-sm text-red-700">{error}</p>}<Button type="submit" disabled={busy} className="mt-6">{busy?"Submitting brief…":firstFree?"Request copywriter — free":"Request copywriter"}</Button></form>}
