"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Field, Input, Select } from "@/components/ui/form-field";
type Vendor={name:string;category:string;phone:string;whatsapp:string|null;location:string|null;deliveryEnabled:boolean;pickupEnabled:boolean;deliveryFeeKobo:number};
export function SettingsForm({vendor}:{vendor:Vendor}){const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const f=new FormData(event.currentTarget);const response=await fetch("/api/dashboard/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),category:f.get("category"),phone:f.get("phone"),whatsapp:f.get("whatsapp"),location:f.get("location"),deliveryEnabled:f.get("deliveryEnabled")==="on",pickupEnabled:f.get("pickupEnabled")==="on",deliveryFeeNaira:f.get("deliveryFee")})});setMessage(response.ok?"Changes saved.":(await response.json()).error);setBusy(false)}return <form onSubmit={submit} className="card p-6 sm:p-8"><h2 className="text-lg font-bold">Business profile</h2><div className="mt-6 grid gap-5 sm:grid-cols-2"><Field label="Business name"><Input name="name" defaultValue={vendor.name}/></Field><Field label="Category"><Select name="category" defaultValue={vendor.category}><option>Food & catering</option><option>Fashion & thrift</option><option>Farm produce</option><option>Other</option></Select></Field><Field label="Phone"><Input name="phone" defaultValue={vendor.phone}/></Field><Field label="WhatsApp"><Input name="whatsapp" defaultValue={vendor.whatsapp||""}/></Field><Field label="Location"><Input name="location" defaultValue={vendor.location||""}/></Field><Field label="Delivery fee (₦)"><Input name="deliveryFee" type="number" defaultValue={vendor.deliveryFeeKobo/100}/></Field></div><div className="mt-6 flex gap-5"><label className="flex items-center gap-2 text-sm font-semibold"><input name="deliveryEnabled" type="checkbox" defaultChecked={vendor.deliveryEnabled}/>Delivery</label><label className="flex items-center gap-2 text-sm font-semibold"><input name="pickupEnabled" type="checkbox" defaultChecked={vendor.pickupEnabled}/>Pickup</label></div>{message&&<p className="mt-5 text-sm font-semibold text-emerald-700">{message}</p>}<Button type="submit" disabled={busy} className="mt-7">{busy?"Saving…":"Save changes"}</Button></form>}
