import { randomBytes } from "node:crypto";
import { hash } from "bcryptjs";
import { cookies } from "next/headers";
import { NextResponse } from "next/server";
import { createSession } from "@/lib/auth/session";
import { db } from "@/lib/db";
type GoogleUser={sub:string;email:string;email_verified:boolean;name?:string};
export async function GET(request:Request){const url=new URL(request.url);const code=url.searchParams.get("code");const state=url.searchParams.get("state");const store=await cookies();const expected=store.get("hemigo_oauth_state")?.value;const verifier=store.get("hemigo_oauth_verifier")?.value;const next=store.get("hemigo_oauth_next")?.value||"/dashboard";for(const name of ["hemigo_oauth_state","hemigo_oauth_verifier","hemigo_oauth_next"])store.set(name,"",{expires:new Date(0),path:"/"});if(!code||!state||!expected||state!==expected||!verifier)return NextResponse.redirect(new URL("/login?error=google-state",request.url));const clientId=process.env.GOOGLE_CLIENT_ID;const clientSecret=process.env.GOOGLE_CLIENT_SECRET;const appUrl=process.env.NEXT_PUBLIC_APP_URL;if(!clientId||!clientSecret||!appUrl)return NextResponse.redirect(new URL("/login?error=google-not-configured",request.url));const tokenResponse=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:clientId,client_secret:clientSecret,redirect_uri:`${appUrl}/api/auth/google/callback`,grant_type:"authorization_code",code_verifier:verifier})});if(!tokenResponse.ok)return NextResponse.redirect(new URL("/login?error=google-token",request.url));const tokens=await tokenResponse.json() as {access_token:string};const profileResponse=await fetch("https://openidconnect.googleapis.com/v1/userinfo",{headers:{Authorization:`Bearer ${tokens.access_token}`}});if(!profileResponse.ok)return NextResponse.redirect(new URL("/login?error=google-profile",request.url));const profile=await profileResponse.json() as GoogleUser;if(!profile.email_verified)return NextResponse.redirect(new URL("/login?error=google-email",request.url));const account=await db.oAuthAccount.findUnique({where:{provider_providerAccountId:{provider:"google",providerAccountId:profile.sub}},include:{user:true}});let user=account?.user??null;if(!user){user=await db.user.findUnique({where:{email:profile.email.toLowerCase()}});if(!user)user=await db.user.create({data:{email:profile.email.toLowerCase(),name:profile.name,passwordHash:await hash(randomBytes(48).toString("base64url"),12),emailVerifiedAt:new Date()}});else if(!user.emailVerifiedAt)user=await db.user.update({where:{id:user.id},data:{emailVerifiedAt:new Date(),name:user.name||profile.name}});await db.oAuthAccount.create({data:{userId:user.id,provider:"google",providerAccountId:profile.sub}})}await createSession(user.id);const hasVendor=Boolean(await db.vendorMember.findFirst({where:{userId:user.id},select:{id:true}}));return NextResponse.redirect(new URL(next==="/dashboard"&&!hasVendor?"/onboarding":next,request.url))}
