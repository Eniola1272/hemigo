"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowLeft } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Field, Input } from "@/components/ui/form-field";
export default function NewEventPage(){const[busy,setBusy]=useState(false);const[error,setError]=useState("");const router=useRouter();async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const form=new FormData(event.currentTarget);const response=await fetch("/api/dashboard/events",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),startsAt:form.get("startsAt"),venue:form.get("venue"),capacity:form.get("capacity")})});const body=await response.json();if(!response.ok){setError(body.error??"Could not create event.");setBusy(false);return}router.push("/dashboard/products/new")}return <div className="mx-auto max-w-2xl"><Button href="/dashboard/events" tone="ghost"><ArrowLeft size={16}/>Events</Button><form onSubmit={submit} className="card mt-5 grid gap-5 p-6 sm:p-8"><p className="eyebrow text-indigo-700">New event</p><h1 className="text-3xl font-bold">Create the event first.</h1><Field label="Event name"><Input name="name" required placeholder="Lagos After Dark"/></Field><Field label="Date and time"><Input name="startsAt" type="datetime-local" required/></Field><Field label="Venue"><Input name="venue" required placeholder="Ikeja, Lagos"/></Field><Field label="Total capacity"><Input name="capacity" type="number" min="1" required/></Field>{error&&<p className="text-sm text-red-700">{error}</p>}<Button type="submit" disabled={busy}>{busy?"Creating…":"Create event and add tickets"}</Button></form></div>}
