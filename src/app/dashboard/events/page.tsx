import { CalendarDays, Plus, ScanLine } from "lucide-react";
import { Button } from "@/components/ui/button";
import { requireVendor } from "@/lib/auth/session";
import { db } from "@/lib/db";
export default async function EventsPage(){const{vendor}=await requireVendor();const events=await db.event.findMany({where:{vendorId:vendor.id},include:{products:true},orderBy:{startsAt:"asc"}});return <div className="space-y-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-3xl font-bold">Events</h1><p className="mt-2 text-slate-500">Create ticket types and check attendees in.</p></div><Button href="/dashboard/events/new"><Plus size={17}/>New event</Button></div>{events.length?<div className="grid gap-4 md:grid-cols-2">{events.map(event=><article key={event.id} className="card p-6"><CalendarDays className="text-indigo-700"/><h2 className="mt-4 text-xl font-bold">{event.name}</h2><p className="mt-2 text-sm text-slate-500">{event.startsAt.toLocaleString("en-NG")} · {event.venue}</p><p className="mt-4 text-sm font-semibold">{event.products.length} ticket types · {event.capacity} capacity</p><Button href={`/dashboard/events/${event.id}/check-in`} tone="secondary" className="mt-5"><ScanLine size={16}/>Open check-in</Button></article>)}</div>:<div className="card p-12 text-center"><CalendarDays className="mx-auto text-indigo-300" size={40}/><h2 className="mt-4 text-xl font-bold">Create your first event.</h2><p className="mt-2 text-slate-500">Then add Regular, VIP, Table, or any ticket type as products.</p></div>}</div>}
