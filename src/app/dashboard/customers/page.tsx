import { Search, Users } from "lucide-react";
import { orders } from "@/lib/demo-data";
import { formatNaira, initials } from "@/lib/utils";

export default function CustomersPage() {
  const unique = orders.slice(0,8);
  return <div className="space-y-7"><div><h1 className="text-3xl font-bold tracking-[-.04em]">Customers</h1><p className="mt-2 text-slate-500">The people who keep coming back for your batches.</p></div><div className="grid gap-4 sm:grid-cols-3"><div className="card p-5"><Users className="text-indigo-700"/><p className="mt-5 text-2xl font-bold">96</p><p className="mt-1 text-sm text-slate-500">All customers</p></div><div className="card p-5"><p className="text-sm text-slate-500">Returning customers</p><p className="mt-5 text-2xl font-bold">38%</p><p className="mt-1 text-xs text-emerald-600">↑ 6% this month</p></div><div className="card p-5"><p className="text-sm text-slate-500">Average order value</p><p className="mt-5 text-2xl font-bold">₦8,460</p><p className="mt-1 text-xs text-slate-500">Across all windows</p></div></div><section className="card overflow-hidden"><div className="flex items-center gap-2 border-b border-slate-200 p-4"><Search size={17} className="text-slate-400"/><input className="w-full bg-transparent text-sm outline-none" placeholder="Search customers"/></div>{unique.map((order,i)=><div key={i} className="flex items-center gap-4 border-b border-slate-100 p-4 last:border-0"><span className="grid size-10 place-items-center rounded-full bg-indigo-50 text-xs font-bold text-indigo-700">{initials(order.customer)}</span><div className="min-w-0 flex-1"><p className="font-semibold">{order.customer}</p><p className="text-sm text-slate-500">{order.phone}</p></div><div className="hidden text-right sm:block"><p className="font-semibold">{(i%4)+1} orders</p><p className="text-xs text-slate-500">{formatNaira(order.amountKobo*((i%4)+1))} spent</p></div></div>)}</section></div>;
}
