import { notFound } from "next/navigation";
import { Navbar } from "@/components/marketing/navbar";
import { MessageThread } from "@/components/messages/message-thread";
import { requireUser } from "@/lib/auth/session";
import { db } from "@/lib/db";
export default async function ConversationPage({params}:{params:Promise<{id:string}>}){const user=await requireUser();const{id}=await params;const[hasVendor,conversation]=await Promise.all([db.vendorMember.findFirst({where:{userId:user.id},select:{id:true}}),db.conversation.findFirst({where:{id,OR:[{participants:{some:{userId:user.id}}},{vendor:{members:{some:{userId:user.id}}}}]},include:{vendor:true,order:{select:{orderNumber:true}},messages:{include:{sender:{select:{id:true,name:true,email:true}}},orderBy:{createdAt:"asc"}}}})]);if(!conversation)notFound();return <main className="min-h-screen bg-slate-50"><Navbar user={{name:user.name||user.email,hasVendor:Boolean(hasVendor)}}/><div className="mx-auto max-w-3xl px-4 py-10"><p className="eyebrow text-indigo-700">{conversation.vendor.name}</p><h1 className="mt-2 text-3xl font-bold">{conversation.subject}</h1>{conversation.order&&<p className="mt-2 text-sm text-slate-500">Regarding order #{conversation.order.orderNumber}</p>}<section className="card mt-7 overflow-hidden"><MessageThread conversationId={conversation.id} currentUserId={user.id} initial={conversation.messages.map(message=>({...message,createdAt:message.createdAt.toISOString()}))}/></section></div></main>}
